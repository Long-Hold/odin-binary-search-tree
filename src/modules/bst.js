class Node {
    constructor(value = null, left = null, right = null) {
        this.value = value;
        this.left = left;
        this.right = right;
    }
}

export class Tree {
    constructor(numArray) {
        this.root = this.buildTree(numArray);
    }

    buildTree(numArray) {
        
    }
}